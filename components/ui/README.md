# UI primitives

Add only reusable engineering primitives here. Project-specific compositions belong closer to their section/page so the visual identity does not collapse into a generic component library.
